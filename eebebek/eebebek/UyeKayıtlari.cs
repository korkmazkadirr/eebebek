using Firebase.Database;
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace eebebek
{
    public class UyeKayıtlari
    {
        /* FirebaseClient FirebaseClient = new FirebaseClient("https://eebebekfirebase-default-rtdb.europe-west1.firebasedatabase.app");
     }
     public async Task<bool> Save(Student student)
     {
         var data = await FirebaseClient.child(nameof(student)).PostAsync(JsonConvert.SerializeObject(student));
         if(string.IsNullOrEmpty(data.Key))
         { 
             return true; 
         }
         return false;*/
     }
    }
